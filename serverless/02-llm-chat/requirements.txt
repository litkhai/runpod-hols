# torch is NOT listed — it ships in the pytorch/pytorch base image.
# Installing it again would add gigabytes and risk a CUDA version mismatch.
# torch 는 적지 않는다. pytorch/pytorch 베이스 이미지에 이미 들어 있다.
# 다시 설치하면 수 GB 가 늘고 CUDA 버전이 어긋날 위험이 있다.

runpod~=1.11.0
transformers~=5.14.1
accelerate~=1.14.0
